pipeline {
    agent any

    stages {

        stage ('test'){
          steps {
            echo 'testing'
          }
        }

        stage ('fetch'){
            steps {
                echo 'fetch'
            }

        }

        stage (''build){
         steps {
            echo 'build'
         }

        }
    }
}